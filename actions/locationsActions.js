export default locationsActions = {
    setMarkerLocation : (name, coords) =>{
        debugger;
        return{
            type: 'SET_MARKER_LOCATION',
            name,
            coords
        }

    }
}